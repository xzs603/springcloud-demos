server:
  port: 6666

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
  profiles:
    active: native

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/

